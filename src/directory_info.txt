This folder only exists to prevent import lib from working when lib has not been installed.
To install, type "pip install -e ." 
This installs the package in editable mode, so changes to the source code
are immediately reflected without reinstalling.